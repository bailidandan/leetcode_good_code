Map<String, type> map = new HashMap<String, type>();
	
	Map<String, Integer> map = new HashMap<String, Integer>();
	for (int i = 0; i < str.length(); i++) {
		slope.put(tmp, slope.getOrDefault(type, 0) + 1);//没有就设置为1.
	}
  
  
  
